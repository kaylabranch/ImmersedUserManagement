export default class User {
  constructor(avatar, name, email, title, department) {
    this.avatar = avatar;
    this.name = name;
    this.email = email;
    this.title = title;
    this.department = department;
  }
}